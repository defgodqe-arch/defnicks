# defnicks 1.0.0
# No configuration is required.
#
# On startup defnicks runs the official TAB command:
# /tab group _DEFAULT_ customtabname %essentials_nickname%
#
# TAB then resolves the Essentials nickname dynamically for each player.
